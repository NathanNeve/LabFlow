# # name: Deploy to GitHub Pages

# # on:
# #   push:
# #     branches: [main]

# # jobs:
# #   deploy:
# #     runs-on: ubuntu-latest
# #     permissions:
# #       contents: write
# #     steps:
# #       - uses: actions/checkout@v4

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: '18.18.0'
# #           cache: 'npm'
# #           cache-dependency-path: code/labflow-frontend/package-lock.json

# #       - name: Install dependencies
# #         working-directory: code/labflow-frontend
# #         run: npm install

# #       - name: Build
# #         working-directory: code/labflow-frontend
# #         run: npm run build
# #         env:
# #           BASE_PATH: '/LabFlow'

# #       - name: Deploy
# #         uses: peaceiris/actions-gh-pages@v4
# #         with:
# #           github_token: ${{ secrets.GITHUB_TOKEN }}
# #           publish_dir: code/labflow-frontend/build
