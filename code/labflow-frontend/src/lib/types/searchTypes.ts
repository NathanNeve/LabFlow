// in deze file worden types gedefinieerd die gebruikt worden in de zoekfunctionaliteit van de applicatie

export interface StalenSearchParams {
    searchCode?: string;
    searchDate?: string;
    filteredStatus?: string;
};